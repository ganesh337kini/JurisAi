# Proxy target for dev (must match backend PORT). Used when VITE_API_URL is not set.
VITE_BACKEND_ORIGIN=http://127.0.0.1:5001

# Optional: full API URL. If unset, dev uses "/api" and Vite proxies to VITE_BACKEND_ORIGIN.
# VITE_API_URL=http://127.0.0.1:5001/api
